# Fiscalberry CLI Dependencies
# Última actualización: 2025-07-19
# Usa las últimas versiones estables de todas las dependencias

# Core dependencies
python-escpos[all]>=3.0
python-socketio[client]
requests
aiohttp

# Utility libraries
appdirs
platformdirs

# Message broker (MQTT + AMQP binding)
paho-mqtt>=1.6.0,<2  # <2: el código usa la API v1 de paho (Client(client_id=...), on_connect(rc)). paho 2.x rompe.
pika>=1.3.0  # Para crear binding AMQP inicial

# Windows-specific dependencies
pywin32; sys_platform == 'win32'