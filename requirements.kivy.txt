# Fiscalberry GUI (Kivy) Dependencies
# Última actualización: 2026-01-19
# NOTA: Se recomienda Python 3.12 para mejor compatibilidad con Kivy
# Python 3.14 puede tener problemas con el parser de archivos .kv

# GUI Framework
kivy[base]>=2.2.0,<2.4.0

# Kivy Windows Dependencies
kivy_deps.sdl2; sys_platform == 'win32'
kivy_deps.glew; sys_platform == 'win32'

# Core dependencies (from CLI)
python-escpos[all]
python-socketio[client]
requests
aiohttp

# Utility libraries
appdirs
platformdirs

# Message broker (MQTT + AMQP binding)
# CRÍTICO: paho-mqtt es el cliente MQTT real (consumer.py/error_publisher.py lo importan).
# Compatible con paho 1.x y 2.x via mqtt_compat (callback_api_version=VERSION1 en 2.x).
paho-mqtt>=1.6.0
# Binding AMQP legacy: SOLO si [RabbitMq] create_amqp_binding=true (default true).
pika>=1.3.0  # solo para crear el binding AMQP inicial

# Windows-specific dependencies
pywin32; sys_platform == 'win32'